import exstream from './index.js'

export default exstream

export const {
  BufferOverflowError,
  CsvParseError,
  CsvStringifyError,
  JsonParseError,
  JsonStringifyError,
  MapAsyncTimeoutError,
  data,
  defer,
  destination,
  errorInfo,
  fromEvent,
  nil,
  pipeline,
} = exstream